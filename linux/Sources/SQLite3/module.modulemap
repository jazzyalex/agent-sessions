module SQLite3 [system] {
    header "shim.h"
    // No `link "sqlite3"`: the executable links libsqlite3.a explicitly (see Package.swift)
    // so the binary has no libsqlite3.so.0 runtime dependency.
    export *
}
