# Marketing Backlog

Pending marketing / promo work — assets, copy, launch. Newest on top.
(Existing published assets are cataloged in [SCREENSHOTS.md](./SCREENSHOTS.md).)

---

## 4.2 — Redesigned-transcript hero assets (screenshots + GIF)
- **Why:** The 4.2 release copy is live (README "What's New", `docs/index.html` meta,
  CHANGELOG), but the README Overview hero and the site hero still show the **4.0
  Session Runway** GIF. 4.2's headline is the redesigned block-based **Session**
  transcript — it has no promo visual yet.
- **Shots to capture** (Light + Dark, `v42-...` per SCREENSHOTS.md naming):
  - Session transcript showing rich Markdown — a message card with headings/lists/
    a **GFM table**/a fenced code block, plus a collapsed tool card and an inline image.
  - The role filter bar (All · You · Agent · Tools · Errors) with the ▲▼ jump-nav
    chevrons visible.
  - The right-click menu (Copy · Copy Block · Speak).
  - Optional GIF: expand a tool card, then step between roles with ▲▼.
- **Then:** resize to ≤1600px long edge, add rows to `Marketing/SCREENSHOTS.md`, and
  swap the README Overview hero + `docs/index.html` hero from the 4.0 runway GIF to
  the new transcript asset (publish the chosen image to `docs/assets/`).
- **Deferred because:** needs the app running, a good sample session, and your eye
  for framing; the 4.2 text copy shipped without it.
- **Source:** flagged during the 4.2 deploy, 2026-07-06.
