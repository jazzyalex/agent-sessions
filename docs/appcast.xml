<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.5</title>
            <pubDate>Wed, 15 Jul 2026 18:56:02 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.5</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>See what each session is costing you.</strong> The Session Runway can now report every active session&#x27;s <strong>API-equivalent spend per hour</strong> — <code>$4.20/h</code> — priced per model, so an orchestrator on Opus driving subagents on Sonnet is costed at what each one actually runs at, not blended. Pick what the runway measures right from the meter: <strong>5-hour</strong> burn, <strong>weekly</strong> share, raw <strong>tokens/hour</strong>, or <strong>dollars</strong>.</li>
<li><strong>The Quota Meter holds still.</strong> It used to grow whenever your pointer crossed it, which made a pinned widget a fight to reposition — the window moved while you were aiming at it. Now the pointer only moves it, and <strong>right-click</strong> brings up its controls. Drag it anywhere; it stays exactly where you put it.</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>Selectable Session Runway rates.</strong> 5-Hour, Weekly, Tokens, or Dollars — chosen from the runway control in the Quota Meter toolbar, and remembered. Dollars falls back to token throughput for any model we can&#x27;t price, rather than dropping the session.</li>
<li><strong>One View-menu entry for the cockpit.</strong> <code>View → Quota Meter / Agent Cockpit</code> now holds the whole choice — Quota Meter, Compact, Full, or <strong>Off</strong> — with ⌘⌥⇧C for the Quota Meter and ⇧⌘M to cycle.</li>
<li><strong>The Quota Meter offers itself.</strong> If you have Codex or Claude sessions but have never switched it on, a card in the session list explains what it does and turns it on in one click. Fresh installs now arrive with it enabled.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>The Quota Meter toolbar is grouped by what its controls do — window destinations lead, the two runway controls fuse into one segmented pill, and pin stands alone behind a separator.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>The Quota Meter no longer walks up the screen.</strong> Near the bottom of a display it grew *upward* when its toolbar appeared, but collapsing kept the raised top edge — so it climbed one toolbar height every time, drifting away from where you parked it.</li>
<li><strong>Compact and the Quota Meter can be closed.</strong> Neither has a titlebar close button, and ⌘W merely beeped, so the only way out was switching to Full or quitting. ⌘W now closes them, as does <code>View → … → Off</code>.</li>
<li><strong>The cockpit&#x27;s provider rows line up with their reset-credits line.</strong> The 5h/Wk row sat centered while the credits line beneath it was flush left.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.5">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.5</a></p>
</div>
            ]]></description>
            <sparkle:version>60</sparkle:version>
            <sparkle:shortVersionString>4.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.5/AgentSessions-4.5.dmg" length="21957800" type="application/octet-stream" sparkle:edSignature="Euiybtl7jE8ocqWm21KiexxCUJ/MGrDlhlMzmUkBPKF73sB2j7+vrJ97zWTcfR+3Lg0QYoUdFIzegqfLrRRCAQ=="/>
        </item>
    </channel>
</rss>