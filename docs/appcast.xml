<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.6.2</title>
            <pubDate>Mon, 20 Jul 2026 09:33:34 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.6.2</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Your Codex history rebuilds once, on the first launch after updating.</strong> The guardian fix below can only reach sessions that are already indexed by re-reading them, so this release re-derives the metadata for your Codex sessions on first launch. While that runs, Codex sessions are missing from the session list <strong>and from search results</strong>, then reappear all at once when it finishes — there&#x27;s no partial fill. A large library takes a while: timed here at about <strong>two and a half minutes</strong> for 3,300 Codex sessions across 5 GB of transcripts, and proportionally less for smaller histories. Claude sessions are untouched throughout, and the rebuild happens exactly once. If your Codex sessions look like they&#x27;ve vanished right after updating, let it finish.</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>Cowork sessions are labelled as Cowork.</strong> Claude&#x27;s Cowork tasks were already in your history, but they wore the same <strong>desk</strong> badge as Claude Desktop&#x27;s Code tab, so there was no way to tell a sandboxed Cowork run from a normal desktop coding session. They now carry their own <strong>cowork</strong> badge. Renaming a task inside Cowork retitles it here without a reindex, and archiving one puts it behind the same archive filter that already covers Claude Desktop — the transcript&#x27;s archive strip included.</li>
<li><strong>Codex&#x27;s sandboxed tasks get their own badge too.</strong> Codex Desktop runs some tasks in a throwaway workspace under <code>~/Documents/Codex/&lt;date&gt;/&lt;slug&gt;/</code> instead of a repo you chose. Those sessions used to look identical to ordinary Codex Desktop work; they now carry a <strong>work</strong> badge, named after Codex&#x27;s own <code>codex_work_desktop</code> marker rather than borrowing Claude&#x27;s Cowork name for a different vendor&#x27;s feature.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>Codex guardian sessions stop duplicating their parent&#x27;s row.</strong> Codex&#x27;s approval-reviewer subagent (&quot;guardian&quot;) sessions used to show up as a second top-level row right next to the session they were reviewing — same title, the same <strong>work</strong> pill, no sign the two were related. They&#x27;re now classified as subagents like any other Codex subagent: they nest underneath their parent, and the <strong>work</strong> pill no longer duplicates onto them. Applying this to sessions you already indexed needs the one-time Codex rebuild described under Highlights.</li>
<li><strong>Resuming a Codex guardian session no longer resumes its parent.</strong> Newer Codex builds recorded a guardian subagent&#x27;s session id as its parent&#x27;s rather than its own, so clicking Resume on a guardian row reopened the parent&#x27;s session. Guardian rows now resume themselves.</li>
<li><strong>Claude usage recovers in seconds, not minutes.</strong> When the Claude CLI refreshes its sign-in while Agent Sessions holds an older cached token, the meter now retries immediately with the fresh token instead of waiting out a multi-minute credential check.</li>
<li><strong>Rate-limit windows no longer black out the meter.</strong> When Anthropic&#x27;s usage endpoint rate-limits (windows can run tens of minutes), Agent Sessions now switches to the claude.ai web path to keep live numbers flowing instead of sitting on stale data.</li>
<li><strong>The spinner tells you why.</strong> The &quot;reconnecting…&quot; cells in the Quota Meter, menu bar, and footer now say what&#x27;s actually happening — &quot;rate limited — retrying…&quot; — instead of spinning anonymously.</li>
<li><strong>Leaked probe sessions are reaped reliably.</strong> A hidden Claude probe could survive for days if its tmux socket vanished; the launch sweep now retries and logs instead of silently skipping.</li>
</ul>
<h3>Reminder: What You Got in 4.6</h3>
<ul>
<li><strong>Read your Claude subscription usage without the CLI — paste a session cookie.</strong> If you don&#x27;t run the Claude CLI, Agent Sessions reads usage over the web instead. On macOS 14+ Safari stopped handing the claude.ai session to other apps, so that path had quietly become a dead end. Now you paste your claude.ai session cookie once in Settings → Usage Tracking: it&#x27;s kept in your Keychain, needs no Full Disk Access, and sign-in never leaves your browser. A <strong>Test now</strong> button confirms it works on the spot.</li>
<li><strong>Paste-a-cookie web usage for Claude.</strong> Settings → Usage Tracking accepts your claude.ai <code>sessionKey</code> (or the whole cookie header) and uses it directly for usage lookups — no Full Disk Access, no scraping. The app only holds the token you hand it, stored in the Keychain. Reading the cookie from Safari is kept as a legacy fallback for anyone it still works for.</li>
<li><strong>The Web API path stops telling signed-in users to &quot;sign in&quot;.</strong> When it couldn&#x27;t find a claude.ai session it advised signing in &quot;at claude.ai (default profile)&quot; — impossible to satisfy on macOS 14/15, where Safari keeps the live cookie in a store apps can&#x27;t read. The messages are now honest and cause-aware, and point to the fix that actually works: paste your session cookie.</li>
<li><strong>The Quota Meter&#x27;s right-click hint stays findable.</strong> In On-Demand mode the &quot;Right-click for controls&quot; hint used to show once and never again — but right-click is the only way back to the toolbar, so it now reappears on every hover.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.6.2">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.6.2</a></p>
</div>
            ]]></description>
            <sparkle:version>63</sparkle:version>
            <sparkle:shortVersionString>4.6.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.6.2/AgentSessions-4.6.2.dmg" length="22097556" type="application/octet-stream" sparkle:edSignature="IIZR1n9rZ1Iz4FAkPfmu7Ld49dlu9PROpi7OJMyZkY3JAiPNsGVmhh906sKapX3m9MasoiR0+0bgljlQeTZWAg=="/>
        </item>
    </channel>
</rss>