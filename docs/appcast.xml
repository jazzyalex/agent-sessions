<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>5.0</title>
            <pubDate>Mon, 17 Aug 2026 20:12:12 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 5.0</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Agents are now plug-in adapters, and adding one is a documented recipe.</strong> Until now every agent was wired in by hand, surface by surface — session list, search, filter pills, Analytics, Settings, launch progress, the enablement notice — which is why an agent could appear in one place and be quietly missing from another, and why adding one meant first finding all those places. Each agent is now described once, in its own adapter, and every surface derives from that description. Measured on a real contribution: adding an agent used to touch 26 shared files; it now touches 12, all listed in [docs/adding-a-session-source.md](adding-a-session-source.md), with tests that fail if one is missed. Qwen Code (below) is the first agent added this way. For users the payoff is consistency — switch an agent off and it leaves every surface together; for contributors it means you can add the agent you use without archaeology.</li>
<li><strong>Qwen Code joins as the thirteenth session source.</strong> Agent Sessions discovers session-ID-named JSONL chats under Qwen&#x27;s local projects store and adds them to browsing, search, filtering, Analytics, saved-session backfill, and the transcript view, including recorded reasoning and tool activity. Rewound branches are reconstructed with Qwen&#x27;s active parent chain, repeated-record fragments and glued JSON objects are recovered, and current custom titles and working directories follow that chain. Settings includes binary and storage-root controls; active-chat resume actions use the installed CLI&#x27;s advertised <code>--resume &lt;id&gt;</code> syntax, with directory-scoped <code>--continue</code> as a safe fallback, while archived histories remain browse-only. The format is verified against real 0.14.3 transcripts. The installed CLI is 0.21.13, but Qwen&#x27;s free OAuth tier was withdrawn on 2026-04-15, so no fresh 0.21.13 transcript could be captured and resume is implemented from installed help and reader evidence with hermetic tests rather than an end-to-end run.</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>A one-time invitation to add a missing agent.</strong> Long-time users see a single dismissible card above the session list pointing at the source-proposal form and the contributing guide, so the manual contribution path is discoverable from inside the app. It appears only after 25 opened sessions or 45 days, never on a fresh install, never above an existing card, and ends permanently on dismissal, on a second &quot;Maybe later&quot;, or as soon as either page is opened. The card reads nothing and sends nothing — it only opens a public page.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>Switching an agent off now keeps it out of filter-view search results.</strong> Search inside the filtered session view ran on a hand-written allow-list that stopped short of several agents, so their sessions could still surface in results after you had disabled them. The allow-list now derives from the same enablement the rest of the app reads, for every agent.</li>
<li><strong>Kimi Code and Grok CLI report when their loading has finished.</strong> Neither agent&#x27;s indexing phases reached the app&#x27;s launch state, so the startup progress could stay incomplete or misreport while their sessions were in fact ready. Launch state now covers all thirteen agents, and an agent you do not have installed settles to ready instead of hanging.</li>
<li><strong>OpenClaw no longer sets Analytics rebuilding on every launch.</strong> OpenClaw&#x27;s enable switch was mirrored into a second stored value that was never seeded from the real setting, so each launch read a change that had not happened — marking Analytics out of date and triggering a refresh nothing asked for. Both now read one setting.</li>
<li><strong>Every agent&#x27;s switch shows the same &quot;restart to apply&quot; notice.</strong> Toggling Hermes, Droid, OpenClaw, Cursor or Pi silently skipped the notice the other agents flash, so the same action gave different feedback depending on which agent you flipped.</li>
<li><strong>Database-backed agent sessions now enter indexed search.</strong> OpenCode and Hermes sessions stored in SQLite shared one file path, so background search ingest could not tell their session identities apart and skipped their transcript text. The ingest path now tracks each session&#x27;s identity, storage path, and content revision independently, refreshes WAL-only changes, pages past stale search hits, preserves the last healthy index on transient database failures, and reconciles moved, unpinned, deleted, or old-root sessions across search and Analytics.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v5.0">https://github.com/jazzyalex/agent-sessions/releases/tag/v5.0</a></p>
</div>
            ]]></description>
            <sparkle:version>68</sparkle:version>
            <sparkle:shortVersionString>5.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v5.0/AgentSessions-5.0.dmg" length="22686524" type="application/octet-stream" sparkle:edSignature="7HV51VPXrVMvUu9N4coezU60vDSxcsNohwyxh0/StinF0odr5bp6OAVTeBsbfgNIYn71Rq+CtYCxUECSi62lBQ=="/>
        </item>
    </channel>
</rss>