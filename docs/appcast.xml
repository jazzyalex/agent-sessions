<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>3.5</title>
            <pubDate>Mon, 06 Apr 2026 17:10:52 -0700</pubDate>
            <description><![CDATA[
<h2>What&#x27;s New in 3.5</h2>
<h3>Features</h3>
<ul>
<li>Transcript toolbar: Export as Markdown button for saving session transcripts as `.md` files.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>Analytics: Index building now derives from `session_meta` instead of file parsing, significantly reducing work.</li>
<li>SwiftUI: Eliminated redundant body evaluations and cached date formatters to reduce CPU usage.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li>Analytics: Index now auto-builds on view appear; suppressed probe cleanup double-refresh.</li>
<li>Concurrency: Captured mutable vars as `let` before `MainActor.run` for Swift 6 strict concurrency compliance.</li>
<li>Unified view: Restored `@ObservedObject` for Gemini indexer in `UnifiedSessionsView`.</li>
<li>Thread safety: Added `NSLock` around cached date formatters in `ClaudeSessionParser` and `SessionIndexer`.</li>
<li>Image Browser: Inline image click now handled in `mouseDown` for reliable open behavior.</li>
<li>Preview.app: Use `/usr/bin/open` instead of `NSWorkspace` API to launch Preview reliably.</li>
</ul>
<h3>Reminder: What You Got in 3.4</h3>
<ul>
<li>Session list: Codex subagent sessions now appear as nested children under their parent in the unified session list. A toggle shows or hides the hierarchy; `Cmd+H` controls visibility from the keyboard.</li>
<li>Agent Cockpit: Active Codex subagent count badge added to HUD rows. `Cmd+Shift+S` toggles subagent display.</li>
<li>Agent Cockpit HUD: Codex active subagent badges now use the runtime `thread_spawn` state DB when available, so open worker agents stay counted even when their rollout files go quiet; passive rollout/`lsof` heuristics remain as a fallback.</li>
<li>Agent Cockpit HUD: Codex runtime subagent counts now resolve the parent session before runtime edge lookup (even when the primary log path is a child rollout), and runtime state DB discovery now honors `CODEX_HOME`/`SessionsRootOverride` before falling back to `~/.codex`.</li>
</ul>
<p>Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v3.5">https://github.com/jazzyalex/agent-sessions/releases/tag/v3.5</a></p>
            ]]></description>
            <sparkle:version>35</sparkle:version>
            <sparkle:shortVersionString>3.5</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v3.5/AgentSessions-3.5.dmg" length="15302956" type="application/octet-stream" sparkle:edSignature="K+4feRRFs6PgkbcMZiwER3uV40MwQHL1n/kMCn6HpD5C1iLz0yHo5fohYQduwEq7gaJNUFngdbn6M9FVMrQABA=="/>
        </item>
    </channel>
</rss>