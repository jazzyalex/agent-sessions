<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.7</title>
            <pubDate>Tue, 04 Aug 2026 15:36:15 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.7</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Kimi Code is the eleventh agent Agent Sessions reads.</strong> Its sessions land in the session list, in search and in Analytics, with tool calls and assistant output rendered in the transcript, a filter pill in the toolbar, and both Copy Resume Command and terminal resume. Agent Sessions switches it on when it finds Kimi Code on your Mac, and Settings → Agents gains a Kimi Code pane for the binary path and storage root.</li>
<li><strong>Active Claude cloud sessions appear in the Quota Meter.</strong> Sessions running on Anthropic&#x27;s infrastructure were invisible outside Claude Desktop; they now sit in the pinned Quota Meter beside your local agents, marked <strong>Cloud</strong> and ordered above them. They carry no burn rate — there is no local transcript to measure — so they report status instead of a runway. Off by default, and offered only once you have saved a claude.ai cookie under Settings → Usage Tracking, since the fetch depends on it.</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>Agent runs with no terminal now reach the Quota Meter.</strong> A <code>claude -p</code> run started by launchd, a shell script, or another app has no controlling terminal, and presence discovery required one — so those sessions never appeared in Live Sessions even though the main window indexed their transcripts normally. They are picked up now.</li>
<li><strong>Analytics counts Pi sessions.</strong> Pi has been a supported source since 3.8 but was missing from the analytics rollups. Its sessions, messages, durations and projects are now included, with a Pi-only filter alongside the others.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li><strong>Turning an agent on or off updates the rest of the app.</strong> Agent Sessions only watched five of the eleven agent switches, so toggling any of the other six left Analytics and the usage meters running on the previous set until relaunch.</li>
<li><strong>A refused Copy Resume Command leaves your clipboard alone.</strong> It cleared the pasteboard before working out whether it had anything to write, so the cases where it declines — a Codex session whose id cannot be resolved, a session with no known working directory — wiped whatever you were holding and replaced it with nothing.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>Pi and Cursor sessions get their working directory back.</strong> After a full parse, a Pi or Cursor session lost the directory it ran in. Two things broke as a result: its resume command came back without the <code>cd</code>, so the resumed agent started wherever your terminal happened to point, and the search index stored no path for it at all, so filtering by project silently stopped matching those sessions. Both are fixed, and sessions indexed before the fix are re-read once in the background after you update, so the repair reaches the history you already have rather than only new sessions.</li>
<li><strong>A resume that fails now tells you.</strong> Every reason a resume could fail — a CLI that could not be found, a Warp that refused to launch, a session whose working directory is unknown — produced an error nothing ever displayed, so all of them looked identical on screen: nothing happened. Failures now come back as an alert. Warp in particular used to report success even when it never opened, and a failed launch would quietly retry a different command that could resume an entirely different session while reporting the one you clicked.</li>
<li><strong>Checking an agent&#x27;s CLI no longer freezes the window.</strong> Probing a binary spawns a login shell and a <code>--help</code> for each candidate path, and eight of the agent coordinators ran that on the main thread with no timeout — so a slow shell profile or an unresponsive binary hung the whole app until it finished. Probes now run off the main thread.</li>
<li><strong>The Codex meter no longer sits on &quot;reconnecting…&quot; forever.</strong> With no Codex CLI run in the previous two days, the background refresh returned before ever making its live usage call — so whenever Agent Sessions was not the frontmost app the meter stayed on &quot;reconnecting…&quot; indefinitely, and only recovered if you focused the window. The live fetch no longer depends on a recent local session file. And when a fetch genuinely comes back empty, the meter now says <strong>no active session</strong> rather than implying a connection it is still waiting on.</li>
<li><strong>Codex sessions containing an auto-review turn reappear in the dollar view.</strong> Codex bills real tokens for its <code>codex-auto-review</code> model, but no price key matched it — and an unpriceable slice makes the whole session unpriceable, so the runway dropped those sessions from the $ view entirely and fell back to tokens per hour without saying why. The model is priced now.</li>
<li><strong>Antigravity code edits and web searches read as tool work again.</strong> <code>CODE_ACTION</code> and <code>SEARCH_WEB</code> carry the same result envelope as the file and command events, but fell through to the parser&#x27;s default branch and rendered as system noise — a code edit was indistinguishable from a checkpoint. Transcripts the CLI truncated upstream are also marked as such now, instead of rendering as though they were complete.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.7">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.7</a></p>
</div>
            ]]></description>
            <sparkle:version>66</sparkle:version>
            <sparkle:shortVersionString>4.7</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.7/AgentSessions-4.7.dmg" length="21968620" type="application/octet-stream" sparkle:edSignature="Sa1Jn7Gex72EvGahmnZ0o/0NPOJLL27cuKoJNz6JIeIDuwIWCwoV/Y7UVELp6dsH5TkSZbOxHagVYJ7V0NecCw=="/>
        </item>
    </channel>
</rss>