<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.0</title>
            <pubDate>Sun, 28 Jun 2026 10:57:57 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.0</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Quota Meter — now with Session Runway.</strong> See at a glance how much of your Codex and Claude 5h and weekly limits is left and when it resets. New in 4.0, <strong>Session Runway</strong> adds live per-session burn-rate bars, so you can spot which active session is eating your quota fastest before it costs you the window. (Formerly &quot;Limits Cockpit.&quot;)</li>
<li><strong>Codex Side Chats</strong> — recover Codex Desktop side chats as searchable rows with a distinct <code>side</code> badge and parent context; filter with <code>#side</code> (and <code>#side phrase</code>), and Copy Session ID copies the parent thread ID.</li>
<li><strong>Antigravity provider</strong> (replaces Gemini CLI) — discovers Antigravity CLI session transcripts, resumes via <code>agy --conversation &lt;id&gt;</code> (with <code>--continue</code> fallback), tracks live sessions, and shows local screenshots in the transcript and image browser.</li>
<li><strong>Restore archived Claude sessions</strong> — Claude Code can archive Desktop sessions but gives you no way to bring them back. Agent Sessions surfaces archived sessions (an <code>archived</code> pill and an archived-only filter) and restores them in place. Because the app is otherwise read-only, restore is an explicit opt-in that&#x27;s off by default — Agent Sessions only writes to Claude&#x27;s files once you enable it.</li>
<li><strong>Claude dynamic workflows</strong> — Claude Code&#x27;s Workflow tool spawns subagents dynamically at runtime; those workflow subagents now nest under the session that launched them with a <code>workflow</code> badge and a fan-out marker on the parent, instead of cluttering the list as standalone rows.</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>Transcript identity strip</strong> — a compact strip showing session identity, <code>side</code>/<code>sub</code> labels, and parent context, so the transcript stays identifiable even when the session list loses focus.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>Quota Meter: moved the cockpit-display settings (5h run-out projection token, Session Runway text size) to the Agent Cockpit Settings tab.</li>
<li>Quota Meter: removed the active/idle session color dots and counters from the header; the All/active/idle filter pills remain in the Full and Compact cockpit views.</li>
<li>Quota Meter: added toolbar Runway/view controls and a Standard / Enlarged size option, and now shows Codex reset credits in the meter and menu bar.</li>
<li>Session list: the Claude agent filter is now a segmented pill matching Codex, and Claude Desktop rows show their Desktop session name.</li>
<li>Unified Window: collapsed hierarchy state now persists across relaunches.</li>
<li>Website: replaced the GitHub Pages landing page with the redesigned product page (updated Antigravity positioning, Quota Meter copy, and production download metadata).</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li>Unified Window: fixed hangs on large Codex histories — the session list no longer stalls diffing full transcript payloads, the Project column no longer re-parses raw transcript JSON during layout, and returning to the app caches hierarchy state instead of rebuilding it.</li>
<li>Claude usage: OAuth/Web refreshes now preserve a recent hard-probe 5h limit and reset time when the soft snapshot only reports weekly quota.</li>
<li>Claude usage: projected-exhaustion alerts now use fractional usage, so small real burns are not missed.</li>
<li>Claude usage: Claude Code 2.x <code>/usage</code> quota gaps and rate-limit responses are treated as unavailable data instead of false 0% readings.</li>
<li>Claude usage: hard refresh now finds Homebrew <code>tmux</code> from GUI launches, stale credentials point to <code>claude /login</code>, and built-in <code>~/.local</code> updater installs are recognized by the per-agent update action.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.0">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.0</a></p>
</div>
            ]]></description>
            <sparkle:version>53</sparkle:version>
            <sparkle:shortVersionString>4.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.0/AgentSessions-4.0.dmg" length="19288183" type="application/octet-stream" sparkle:edSignature="rLi+TLSM8q7FQL54TkW6vS7nmffTsyvb7iuJp2LhOUmZkQ80FjP0cy7QFSwrQGAesz9JKLF5G5pUtrGLkexdCw=="/>
        </item>
    </channel>
</rss>