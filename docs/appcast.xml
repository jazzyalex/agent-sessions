<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>5.0.2</title>
            <pubDate>Sat, 22 Aug 2026 14:22:21 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 5.0.2</h2>
<h3>Improvements</h3>
<ul>
<li><strong>Cursor stops re-reading its database on every refresh.</strong> Sessions that live only in <code>store.db</code> were being run through the JSONL transcript parser on each pass, which could never find anything there.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>OpenCode and Copilot sessions carry their own ID again.</strong> OpenCode moved to a single shared <code>opencode.db</code>, and the transcript toolbar still read the ID off the file path — so every OpenCode session called itself &quot;code&quot; and Copy Session ID handed you <code>opencode</code>. Both now read the session&#x27;s own id.</li>
<li><strong>A live OpenCode or Hermes session keeps growing while you watch it.</strong> Their freshness check looked at the database file&#x27;s timestamp, but SQLite writes land in the <code>-wal</code> sidecar first, so a transcript you had open froze at whatever it held when you selected it. Cursor was checked the same way.</li>
<li><strong>Subagent reports read as text, not as an envelope.</strong> An OpenCode <code>task</code>, a Hermes <code>delegate_task</code> and a Qwen tool result each arrive wrapped — raw <code>&lt;task_result&gt;</code> XML or a one-key JSON blob — and the transcript rendered the wrapper. The report itself is shown now, with the child session&#x27;s id on the OpenCode card. Sessions indexed before this update pick it up when they are next re-read.</li>
<li><strong>Copy Session ID on an OpenClaw session pastes the id.</strong> It came with the <code>openclaw:&lt;agent&gt;:</code> prefix Agent Sessions uses to keep its agents apart, which nothing outside the app accepts.</li>
<li><strong>Pi subagent runs nest under the session that spawned them.</strong> Pi records the parent id with the transcript filename&#x27;s timestamp still on the front, so the match never landed and subagents sat at the top level.</li>
</ul>
<h3>Reminder: What You Got in 5.0</h3>
<ul>
<li><strong>Agents are plug-in adapters now.</strong> Each agent used to be hand-wired into every surface — list, search, pills, Analytics, Settings — so it could show up in one and be missing from another. Now an agent is described once and every surface reads that description. Adding one dropped from 26 shared-file edits to 12, each listed in <a href="https://github.com/jazzyalex/agent-sessions/blob/main/docs/adding-a-session-source.md">the guide</a> with a test that fails if you miss it. Bring the agent you use.</li>
<li><strong>Qwen Code is the thirteenth agent.</strong> Local Qwen chats land in browsing, search, Analytics and the transcript view — reasoning, tool calls, and rewound branches reconstructed along the active parent chain. Active chats resume in the CLI; archived ones stay browse-only. Format verified against real 0.14.3 transcripts (newer builds wait on a Qwen account — the free tier is gone).</li>
<li><strong>The app asks for help adding your agent — once.</strong> A dismissible card points long-time users at the proposal form and contributing guide. It shows up late, takes no for an answer, and reads and sends nothing.</li>
<li><strong>Disabled agents no longer sneak into filter-view search results.</strong> The search allow-list was hand-written and stopped short of several agents; it now derives from the same enablement everything else reads.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v5.0.2">https://github.com/jazzyalex/agent-sessions/releases/tag/v5.0.2</a></p>
</div>
            ]]></description>
            <sparkle:version>70</sparkle:version>
            <sparkle:shortVersionString>5.0.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v5.0.2/AgentSessions-5.0.2.dmg" length="22825258" type="application/octet-stream" sparkle:edSignature="Z4gxydTEkifoBAHRMb/JYL4oB5FK2gXLikOXhvyNqPMsegM+jG6dyhsPgx1LGFZj70SYkeqeERC0E60VxndjCw=="/>
        </item>
    </channel>
</rss>