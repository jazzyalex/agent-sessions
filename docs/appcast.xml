<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.1</title>
            <pubDate>Fri, 03 Jul 2026 18:09:14 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.1</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>The Instant release.</strong> This release is a top-to-bottom performance program: transcripts open instantly, sorting is instant, search is indexed, scrolling is smooth, and the app is dramatically quieter when idle.</li>
<li><strong>Transcripts open instantly</strong> — even monster sessions. Opening a session paints the newest content immediately and completes the rest in the background; the multi-second (sometimes 30s+) beachball on large histories is gone. Jumping to older content (search hits, deep links, first prompt) loads the needed portion on demand.</li>
<li><strong>Instant sort and smoother lists</strong> — clicking a sort column re-orders in place instead of re-running the whole pipeline (~7s → ~0.5s on large histories), fast scrolling no longer stutters, and scrubbing the list no longer stalls the transcript or the HUD.</li>
<li><strong>Indexed full-text search</strong> — the search corpus is now ingested into the SQLite FTS index in the background, so searches hit the index instead of re-scanning files; recently-changed sessions are still scanned live so results are never stale. Selecting a result from a search-filtered list auto-jumps to the first match.</li>
<li><strong>Quieter when idle</strong> — live-session probing, HUD rebuilds, and indexing are now gated and deduplicated; idle CPU and &quot;Using Significant Energy&quot; pressure are sharply reduced.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>Session list: row rendering diet — shared date formatting, precomputed pills, and memoized project names keep scrolling smooth at large row counts.</li>
<li>Transcript: live-tail rebuilds are throttled while the app is inactive and catch up instantly on activation.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li>Transcript: Antigravity and GitHub Copilot sessions now render inline images that were previously detected but never displayed.</li>
<li>Session list: toggling &quot;Hide zero-message sessions&quot;, &quot;Hide low-message sessions&quot;, or housekeeping visibility now refreshes OpenClaw and Antigravity results immediately instead of waiting for an unrelated refresh.</li>
<li>Session list: archived-pill behavior is pinned for switch-branch Claude Desktop sessions.</li>
<li>Quota Meter: the Session Runway overflow label now always reads &quot;+N sessions&quot; instead of splitting into burn/idle &quot;bursts&quot;.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.1">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.1</a></p>
</div>
            ]]></description>
            <sparkle:version>54</sparkle:version>
            <sparkle:shortVersionString>4.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.1/AgentSessions-4.1.dmg" length="19284440" type="application/octet-stream" sparkle:edSignature="/k3q0sUGl+0xj9I3Gdqk/Yh80NccnpLrwz1Odq7shQ38KbcBY314yiOnRpJeA1gZFbctdSmNzBp58P50ZSTGCQ=="/>
        </item>
    </channel>
</rss>