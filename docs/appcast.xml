<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.8</title>
            <pubDate>Fri, 14 Aug 2026 12:48:58 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.8</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Grok CLI is the eleventh current agent source Agent Sessions reads.</strong> Its sessions land in the session list, in search, in Analytics and in the Image Browser, with tool calls, reasoning and assistant output rendered in the transcript, a filter pill in the toolbar, and both Copy Resume Command and terminal resume. Agent Sessions switches it on when it finds Grok CLI on your Mac, and Settings → Agents gains a Grok CLI pane for the binary path and storage root. Subagent runs are shown under the session that spawned them — Grok records that parentage in the parent&#x27;s own tree, so <code>grok sessions list</code> does not show them at all. Schema verified against 141 real sessions. Contributed by [@thedavidweng](https://github.com/thedavidweng) in [#55](https://github.com/jazzyalex/agent-sessions/pull/55).</li>
</ul>
<h3>Features</h3>
<ul>
<li><strong>Analytics counts every agent you have enabled.</strong> The rollup ran on a hand-written list of sources, and Cursor and OpenClaw were never on it — their sessions were indexed, searchable and visible in the list, but contributed nothing to any total, chart or project breakdown, and nothing reported the gap. Analytics now derives from the full set of sources, so every agent counts and a new one cannot be forgotten. Cursor also gains the agent-picker entry it never had. Existing installs rebuild their analytics once on the next launch to pick up the two missing sources; it reads the session index and touches no files.</li>
<li><strong>Inline images from a Grok session now appear.</strong> A Grok transcript can carry a real attachment inlined as a data URI, and four separate gates each dropped it silently — so a session with an image showed one in neither the transcript nor the Image Browser. All four are now exhaustive, and the same scanner is extended to Kimi, Pi, Hermes and Cursor, which previously could only ever render a placeholder.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li><strong>Session-Bench now includes a shareable v0.3 poster.</strong> The launch article shows the full leaderboard graphic, while the benchmark page keeps it available in a compact download section beneath the scored table.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>An unrelated binary earlier in your PATH no longer masks the real CLI.</strong> Cursor, Kimi, Pi and Grok searched candidate locations group by group and returned the first executable they found, even when it did not advertise the flags the CLI needs — so a same-named binary in the login shell&#x27;s PATH won before the actual install directory was ever examined, reporting its version as the agent&#x27;s and refusing resume outright. This one is live rather than theoretical for Grok: Homebrew ships an unrelated <code>grok</code> regex utility, while Grok CLI arrives as the <code>grok-build</code> cask. Each source now searches one ordered list, and a binary that advertises the right flags always wins.</li>
<li><strong>Analytics no longer reports &quot;no sources enabled&quot; over data it has.</strong> Its empty state, the banner warning that totals exclude an agent, and the agent picker were three more hand-written lists, each stopping at a different source. With Grok, Cursor or OpenClaw as your only enabled agent, Analytics claimed nothing was enabled while showing that agent&#x27;s data, and the picker could not isolate any of the three. All three lists now derive from one source of truth.</li>
<li><strong>A Grok session shows the title its own sidecar records.</strong> Grok writes a generated title, but its transcripts open with an injected context preamble rather than your prompt — so once the transcript loaded, the title fell through to that preamble and displayed literal <code>&lt;user_query&gt;</code> markup or the opening line of an assistant reply.</li>
<li><strong>Grok transcripts open with content in them.</strong> The transcript view keeps one layer per provider and reveals the right one, and Grok had no layer — its indexer was wired up and then never used, which no compiler could catch. A test now checks that every source has one.</li>
<li><strong>A Grok session&#x27;s message count matches its transcript.</strong> The count came from a sidecar field that includes system and reasoning records, both of which render as meta — so a transcript of nothing but meta records claimed three messages and slipped past the filters that hide empty and low-message sessions.</li>
<li><strong>The Image Browser credits the prompt that produced an image, not the scaffolding around it.</strong> Agents inject housekeeping blocks into the conversation as if they were your messages — an AGENTS.md preamble, <code>&lt;environment_context&gt;</code>, a <code>&lt;system-reminder&gt;</code>. The transcript already knew to look past those when deciding which prompt an image belongs to; the Image Browser did not, so an image could be filed under a <code>&lt;system-reminder&gt;</code> there while the transcript filed the same image under the real prompt. Claude injects those blocks mid-conversation, so this was not limited to the opening of a session. Both now agree.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.8">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.8</a></p>
</div>
            ]]></description>
            <sparkle:version>67</sparkle:version>
            <sparkle:shortVersionString>4.8</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.8/AgentSessions-4.8.dmg" length="22260606" type="application/octet-stream" sparkle:edSignature="mEgKe1I3oTdsMYzUK8j1QpHOFVg2mbCW7wfc3JoOHv64VrQtWAr9modl7LAXl85fLHkWbcNCVlGWwE1wQwv5Aw=="/>
        </item>
    </channel>
</rss>