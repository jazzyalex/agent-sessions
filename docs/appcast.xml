<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AgentSessions</title>
        <item>
            <title>4.6.4</title>
            <pubDate>Tue, 21 Jul 2026 11:02:51 -0700</pubDate>
            <description><![CDATA[
<style>
.rn { font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif; font-size: 13px; line-height: 1.55; color: #1d1d1f; -webkit-font-smoothing: antialiased; padding: 2px; }
.rn h2 { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; margin: 0 0 2px; }
.rn h3 { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #86868b; margin: 18px 0 6px; }
.rn h3.hl { color: #0071e3; }
.rn ul { margin: 0; padding-left: 18px; }
.rn li { margin: 5px 0; }
.rn ul.highlights li { margin: 9px 0; }
.rn strong { font-weight: 650; }
.rn code { font-family: ui-monospace, "SF Mono", Menlo, monospace; font-size: 0.9em; background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 5px; }
.rn a { color: #0071e3; text-decoration: none; }
.rn .more { color: #86868b; font-size: 12px; margin-top: 16px; }
@media (prefers-color-scheme: dark) {
  .rn { color: #f5f5f7; }
  .rn h3 { color: #98989d; }
  .rn h3.hl { color: #2997ff; }
  .rn code { background: rgba(255,255,255,0.13); }
  .rn a { color: #2997ff; }
}
</style>
<div class="rn">
<h2>What&#x27;s New in 4.6.4</h2>
<h3 class="hl">Highlights</h3>
<ul class="highlights">
<li><strong>Compact and Full Agent Cockpit are retired; the Quota Meter is the only mode.</strong> The two larger cockpit modes carried a session list, search, By Project grouping and filter pills that duplicated what the main window already does better, so they are gone and the Quota Meter absorbs what people actually used it for. If you last quit in Compact or Full, you land on the Quota Meter — no setting to restore, nothing to reconfigure, and the old modes&#x27; leftover preferences and window positions are cleared for you on launch. Where you put the Quota Meter is untouched.</li>
<li><strong>Two keyboard shortcuts change.</strong> <strong>⇧⌘M</strong> (&quot;Cycle Cockpit View&quot;) is gone — cycling means nothing with one mode. <strong>⌘⌥⇧C</strong> now shows and hides the Quota Meter, where it used to select the mode and bring the window forward; pressing it while the Quota Meter is on screen hides it.</li>
<li><strong>The View menu is a single Quota Meter toggle</strong>, replacing the three-mode submenu, and <strong>Settings → Agent Cockpit is now Settings → Quota Meter</strong> — its Compact Mode and Full Mode sections go with the modes they configured.</li>
<li><strong>Gone with the modes:</strong> the session search field and ⌘K, By Project grouping, the All/Active/Idle filter pills, ⌘1–9 and ⌘0 row focus, the row context menu, the full-mode toolbar row, and the Full/Compact quota footer with its hover drawer. The Quota Meter keeps its provider limit rows, Session Runway drawer, chrome modes, pin, enlarged text, the runway and probe popovers, the open-app and settings buttons, and ⌘W.</li>
</ul>
<h3>Bug Fixes</h3>
<ul>
<li><strong>The Quota Meter&#x27;s text-size button no longer disappears when you switch to Standard.</strong> Standard text makes the window about 30pt narrower, and the toolbar used to shed that button first — hiding the only control that switches back to Enlarged, so the setting was one-way from inside the window. The toolbar now narrows before it drops anything: the Session Runway control gives up its presentation half first, and every button stays put.</li>
</ul>
<h3>Reminder: What You Got in 4.6</h3>
<ul>
<li><strong>Read your Claude subscription usage without the CLI — paste a session cookie.</strong> If you don&#x27;t run the Claude CLI, Agent Sessions reads usage over the web instead. On macOS 14+ Safari stopped handing the claude.ai session to other apps, so that path had quietly become a dead end. Now you paste your claude.ai session cookie once in Settings → Usage Tracking: it&#x27;s kept in your Keychain, needs no Full Disk Access, and sign-in never leaves your browser. A <strong>Test now</strong> button confirms it works on the spot.</li>
<li><strong>Paste-a-cookie web usage for Claude.</strong> Settings → Usage Tracking accepts your claude.ai <code>sessionKey</code> (or the whole cookie header) and uses it directly for usage lookups — no Full Disk Access, no scraping. The app only holds the token you hand it, stored in the Keychain. Reading the cookie from Safari is kept as a legacy fallback for anyone it still works for.</li>
<li><strong>The Web API path stops telling signed-in users to &quot;sign in&quot;.</strong> When it couldn&#x27;t find a claude.ai session it advised signing in &quot;at claude.ai (default profile)&quot; — impossible to satisfy on macOS 14/15, where Safari keeps the live cookie in a store apps can&#x27;t read. The messages are now honest and cause-aware, and point to the fix that actually works: paste your session cookie.</li>
<li><strong>The Quota Meter&#x27;s right-click hint stays findable.</strong> In On-Demand mode the &quot;Right-click for controls&quot; hint used to show once and never again — but right-click is the only way back to the toolbar, so it now reappears on every hover.</li>
</ul>
<p class="more">Full release notes: <a href="https://github.com/jazzyalex/agent-sessions/releases/tag/v4.6.4">https://github.com/jazzyalex/agent-sessions/releases/tag/v4.6.4</a></p>
</div>
            ]]></description>
            <sparkle:version>65</sparkle:version>
            <sparkle:shortVersionString>4.6.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/jazzyalex/agent-sessions/releases/download/v4.6.4/AgentSessions-4.6.4.dmg" length="21477841" type="application/octet-stream" sparkle:edSignature="i9U6stG9W3sS22HopHFVvNs6k8zKNT/0Exf1ZDJIbwS/BWbhaUDJUbVJch4uvlQQhYz1RCsU3guQn1qfvYrBCw=="/>
        </item>
    </channel>
</rss>