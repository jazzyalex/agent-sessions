## January 2026

- Preferences: Added a Session View toggle for “Show inline image thumbnails in Session view”.
- Session view: Inline image thumbnails now support hover popover previews and click open in the Image Browser (auto-selecting the clicked image); the inline thumbnail context menu includes Open in Preview and omits Navigate to Session.
- Session view: Added an Images toolbar pill to toggle inline images and jump between prompts that contain images.
- Session view: Inline image thumbnails now ignore data URL strings outside `image_url` payloads to avoid empty placeholders.
- Session view: Codex `<image name=[Image #…]>` markers now render as `[Image #…]` for cleaner copy/paste.
- Image Browser: Double-click opens the selected image in Preview; Space opens Quick Look.
- Appearance: Added a toolbar button to toggle Dark/Light mode and View menu actions for Toggle Dark/Light and Use System Appearance.
- Appearance: Image Browser and auxiliary windows now follow system light/dark changes immediately when using System appearance, and update instantly when switching Light/Dark in Settings or the View menu.
- Images: Image Browser status bar no longer shows “Scanning …” after scanning completes.
- Images: Image Browser project scan progress no longer reports “224/224” while the final session is still scanning.
- Images: Image Browser project scope now always includes the selected session to avoid missing images.
- Menu: Removed the separator between Image Browser and Saved Sessions, and renamed “Saved Sessions…” to “Saved Sessions”.
- Images: Codex sessions with embedded base64 images now show an Image Browser in the main toolbar that opens a thumbnail gallery with preview, save actions, and optional project-wide scope.
- Images: Navigating to a session from the image browser now focuses the main window and jumps to the related user prompt.
- Images: Navigating to a session now brings the main window forward, highlights the image prompt, and restores Tab focus cycling.
- Images: The Image Browser moved to the main toolbar and View menu, and thumbnails now support Copy Image.
- Images: Opening the Image Browser with no images now shows an empty state inside the browser.
- Images: Thumbnails now include a “Copy Image Path” action for terminal/CLI pasting.
- Preferences: Added Security & Privacy and License links in Settings → About.
- Preferences: Made the Settings → About updates section more compact.
- Preferences: Updated the Droid pane with binary detection and version check controls.
- Preferences: OpenCode CLI auto-detection now works for npm installs and checks common pip/pipx install locations on macOS (including `~/Library/Python/*/bin`).
- Copilot: Tool output now renders escaped newlines as real newlines.
- Parsing: Gemini/OpenCode tool outputs now preserve exit codes and mark non-zero exits as errors.
- Parsing: Droid stream-json now parses numeric timestamps, call IDs, and tool error flags.
- Search: Added SQLite full-text indexing for faster global search after analytics indexing completes.
- Search: Instant search now indexes full tool inputs and outputs for recent sessions (last 90 days), reducing the need to wait for background scanning.
- Search: Tool output indexing now redacts embedded base64/data URL blobs to keep search responsive and the index compact.
- Search: Unified Search now behaves like Apple Notes-style search: fast across all sessions, within a specific session, and compatible with all filters.
- Search: Instant search now uses token-prefix matching by default (for example, `magic` behaves like `magic*`) to improve identifier and structured-text recall without trigram/substr indexing.
- Search: Background scanning of large tool outputs is now opt-in by default, keeping Instant search more responsive (Settings → Advanced).
- Search: Instant search now falls back to legacy matching for sessions not indexed yet, preventing missing results during index warmup.
- Search: Instant indexing now samples long assistant messages and tool inputs (head + middle + tail) to reduce false negatives.
- Indexing: Analytics refresh is now incremental (skips unchanged files and removes deleted ones), reducing repeated startup work.
- Startup: When Codex sessions are already indexed, the app now shows the hydrated list immediately while scanning for newly created sessions in the background.
- Search: Incremental refresh now backfills missing per-session search data so Instant search becomes complete over time.
- Search: Instant indexing now captures more of long tool outputs (head + middle + tail slices) and keeps active Codex sessions searchable while they are updating.
- Sessions: Auto-select the first session on launch so the transcript pane isn’t blank.
- Sessions: Large session message counts no longer jump while background parsing completes.
- Sessions: Hide housekeeping-only sessions by default; turn off “Hide housekeeping-only sessions” in Settings → General to show them.
- Sessions: Filter toggles now use consistent “Hide …” labels, and “Hide 1–2 message sessions” no longer hides 0-message sessions unless “Hide 0-message sessions” is also enabled.
- Onboarding: “Sessions Found” counts now reflect your current filter settings.
- Onboarding: Onboarding counts now use the system font.
- Usage: Claude probe auto-delete now removes failed/empty probe sessions.
- Usage: Claude probe cleanup now requires validated probe evidence and the cleanup copy clarifies that normal sessions are untouched.
- Preferences (Usage Probes): Moved “Show system probe sessions for debugging” to the bottom of the pane.
- Usage Tracking: Disabling Codex/Claude tracking now also disables their probes/refresh actions and hides them from the menu bar.
- Sessions: The unified list now shows a live session count that updates while indexing and when filters/search change.
- UI: Consolidated unified-window status indicators into a single cockpit-style footer.
- UI: In dark mode, the cockpit footer now uses a transparent HUD style with crisp borders instead of a solid fill.
- UI: Menu bar usage now uses the same monospace, logo-forward cockpit styling as the footer.
- Menu Bar: Reset menu items now include weekday; menu bar label can hide reset indicators per provider.
- Menu Bar: Removed pill backgrounds from the menu bar label for a cleaner, more native look.
- Menu Bar: When usage data is stale, reset indicators now show “n/a” and Claude usage can surface a “Setup required” state after Claude Code updates.
- Usage: Claude usage refreshes automatically after wake when visible, and the menu bar label shows a spinner while probes run.
- Usage: Hardened Claude and Codex tmux probes with a startup sweep to clean up stalled sessions and avoid orphaned CLI processes.
- Preferences: The Menu Bar pane now groups label options into sections and aligns toggles consistently.
- Usage: Time-only reset strings now roll forward to the next day to avoid showing stale "<1m" countdowns.
- Onboarding: Replaced the onboarding flow with a four-slide tour covering sessions, agent enablement, workflow tips, and usage tracking.
- Onboarding: The tour now shows all supported agents and discovered sessions regardless of Sessions toolbar filters; disabled agents appear as inactive.
- Onboarding: Refined the tour’s primary icon and Next button colors to better match native macOS accents.
- Onboarding: Fixed a crash that could occur during the onboarding tour for some users.
- Transcript: Reduced terminal-view stutter by debouncing rebuilds and updating match highlights without full reloads.
- Transcript: Color view is now called Session view and presents a colored timeline with improved formatting.
- Transcript (Session view): Use system font for narrative blocks; keep tool call blocks monospaced.
- Transcript (Session view): Blocks now render as rounded cards with subtle tints and left accent borders.
- Transcript (Session view): User prompts now use a thicker accent rail, slightly brighter tint, and semibold first-line text for readability.
- Transcript (Session view): Assistant blocks now use per-agent brand tints and the role legend matches transcript accents.
- Transcript (Session view): Find highlights now mark matched substrings and add a line indicator for easier scanning.
- Transcript (Session view): Codex review blocks now render as labeled Review meta entries instead of user prompts.
- Transcript (Session view): System reminder and local-command caveat blocks now render as meta lines so user prompts stay visible.
- Transcript (Session view): Claude local-command tag-only blocks now render as Local Command meta lines.
- Transcript (Session view): Local Command meta blocks now use a thin blue accent strip.
- Transcript (Session view): Request interrupted-by-user markers now use a thin blue strip and regular text.
- Transcript: Clearing Find now immediately clears match highlights.
- Transcript: Removed the duplicate Jump to First Prompt control from the transcript toolbar.
- Sessions list: The Size column can now be sorted.
- Sessions list: Right-click a session to copy its session ID to the clipboard.
- Sessions: Resume actions now include the selected terminal app in the menu label.
- Search: Multi-word Unified Search queries now behave like phrase searches (for example, `exit code`) to match transcript navigation and avoid accidental boolean parsing.
- Search: Unified Search highlights now follow token-phrase matches across punctuation/newlines, and Session view shows visible vs total match counts when role filters are active.
- Search: Global search now accepts quoted `repo:` and `path:` filters, and background deep scans prewarm opened sessions at low priority.
- Search: The Search Sessions menu item and ⌥⌘F shortcut now reliably focus the global search field.
- Search: Unified Search (⌥⌘F) highlights matches in the selected transcript and supports match-to-match navigation via a toolbar pill.
- Transcript: Find in Transcript (⌘F) opens a local find bar with an independent query and navigation.
- Transcript: Unified Search now auto-jumps to the first match after typing or switching sessions, and local Find uses a solid blue current-match marker aligned with the match.
- Transcript: Session view now auto-scrolls to the last user prompt by default, with a Unified Window setting to choose first vs last user prompt.
- Search: Claude sessions now keep transcript match highlights in sync with the active search query.
- Search: Unified Search accepts quoted repo/path filters with spaces.
- Transcript: Session view now shows match counts for the active Unified Search.
- Transcript: Session view search markers now attach only to matching wrapped lines and replace the block accent on those lines.
- Transcript: The Unified Search navigation pill appears only when Unified Search has a free-text query.
- Transcript: Remove the ghost control inside the Unified Search navigation pill.
- Search: Tooltips now include keyboard shortcuts for search fields and navigation arrows.
- Analytics: The By Agent card now auto-scales rows and falls back to an internal scroll when space is tight.
- UI: Pane sizes now persist when switching between vertical and horizontal split layouts.
- UI: Session list rows are taller with monospaced typography, lighter message counts, and softer timestamp gray, plus footer-blue selection accents.
- UI: Increased session list row height to 48px for easier scanning.
- UI: Codex now uses a blue brand accent distinct from the cockpit footer tint; the transcript toolbar spacing is tighter, and the terminal view adds a subtle top border.
- Toolbar: Refined agent tabs, regrouped toolbar icons, and added a pill-style layout toggle alongside theme and settings controls.
- Toolbar: Action icons now remain visible and accessible when a project filter is active, including via the overflow menu.
- UI: Updated Codex and Claude brand colors to deep blue and warm brown for clearer agent identity.
- Transcript: View mode buttons now read Session/Text/JSON, use HIG-style leading padding, and space the session ID control.
- Transcript: Toolbar controls now use monospaced typography to match the session list.
- Transcript: Terminal find highlights no longer linger when the Find query is empty.
- Transcript: Toolbar filters now use dot + count labels with compact navigation chevrons.
- Transcript: Tool calls and outputs now render as readable text blocks (commands, paths, stdout, stderr) instead of JSON wrappers.
- Transcript: Tool navigation now treats call+output as one item, tool output blocks no longer repeat the tool label, and tool/error block padding is consistent.
- Layout: New installs default to the vertical split layout.
- Onboarding: The tour now opens in a standard window with a close button.
- Transcript (Session view): User prompt text now uses regular weight; emphasis comes from the accent strip.
- Transcript (Session view): Accent strips now align to their block padding.
- Transcript (Session view): Block accents no longer bleed into inter-block spacing.
- Transcript (Session view): Accent strips now sit outside text bounds.
- Transcript (Session view): User prompts use the base system font size with matching left/right accent strips; the system preamble keeps a single left strip.
- Transcript (Session view): Reduced the user prompt Optima font size bump from +2pt to +1pt.
- Transcript (Session view): Turn-aborted system notices embedded in user prompts now render as system/meta blocks instead of user prompts.
- Transcript (Session view): User prompt text now uses semibold weight.
- Claude: Opening sessions with embedded base64 blobs (for example, Chrome MCP screenshots) no longer stalls the UI.
- Parsing: Codex thread rollback events now show readable text in timelines.
- Parsing: Claude Code 2.1.9 sessions treat `queue-operation`/`file-history-snapshot` events as metadata; refreshed stage0 fixtures to match new keys and event types.
- Parsing: Gemini CLI 0.24.0 sessions include `model`/`tokens`/`thoughts`; refreshed stage0 fixtures and bumped verified version.
- Parsing: Copilot sessions now treat `assistant.turn_start/end`, `tool.execution_start`, and `session.truncation` as metadata; refreshed stage0 drift fixture to include these types.
