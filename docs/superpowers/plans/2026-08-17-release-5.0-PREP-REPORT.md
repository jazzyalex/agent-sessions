# 5.0 release prep — report

**Date:** 2026-08-17
**Scope:** prepare (not deploy) the 5.0 release. Nothing committed, pushed, branched, or
version-bumped. `CFBundleShortVersionString`, build numbers, the appcast, and Homebrew
were not touched — `deploy` owns those.

**Working tree note:** two other agents' approved-but-uncommitted work was already in the
tree (Grok parser changes, steward program files, CONTRIBUTING/agents.md/README edits,
`.github` templates, `skills/agent-session-format-check`). All of it was left in place and
built on top of.

---

## 1. The 4.9 → 5.0 retarget — every hit classified

Grep run: `4\.9` across `AgentSessions/`, `AgentSessionsTests/`, `docs/`, `README.md`,
`agents.md`, `skills/`, `scripts/`.

### Retargeted (release-bound)

| Site | Before | After |
|---|---|---|
| `AgentSessions/Model/SessionSource.swift:68` | `case .qwen: return "4.9"` | `"5.0"` |
| `AgentSessions/Onboarding/Models/WhatsNewCatalog.swift` teaser key | `"4.9": "Qwen Code joins…"` | `"5.0"` key, copy rewritten (below) |
| `AgentSessionsTests/OnboardingFeedbackTriggerTests.swift:216` | `testQwenReleaseHas49TeaserAndProviderHighlight` | renamed `…Has50…`, retargeted to `"5.0"` |
| `AgentSessionsTests/OnboardingContributeCardTests.swift` ×4 (`:21`, `:92`, `:102`, `:256`) | `"4.9"` current-version fixtures | `"5.0"` |
| `docs/adding-a-session-source.md:57-58` | "the upcoming **4.9** release… can also use `\"4.9\"`" | 5.0 |
| `docs/agent-support/agent-support-matrix.yml:1` | `agent_sessions_version: "4.9-unreleased"` | `"5.0-unreleased"` |
| `README.md:102` | "Qwen Code sessions (shipping in 4.9)" | "(new in 5.0)" |

### Left alone (historical records of decisions made at the time)

- `docs/superpowers/plans/2026-08-14-task0-spike-REPORT.md:201` — records that `"4.9"`
  fixed the fake-source spike. A record of what happened, not a live pin.
- `docs/superpowers/plans/2026-08-17-qwen-integration-REVIEW.md:128-129, 247` — the review
  reasoned about 4.9 as the then-upcoming release. Historical.
- `docs/superpowers/plans/2026-08-17-steward-program-PLAN.md:24` — "Contribute card is
  live (v4.9)". Historical plan text.
- `scripts/probe_scan_output/**` — timestamps and probe JSON that merely contain the
  digits. Not versions.

### Not a "4.9" hit, but checked for release-boundness

- `AgentSessionsTests/NewProviderDiscoverabilityTests.swift:223` pins `"2.9"` as a real app
  version no provider claims, and `:220` pins `"99.0"` as unknown. Neither collides with
  `"5.0"`; both still pass.
- `WhatsNewCatalogTests.testTeaserPresentForCurrentRelease` /
  `testHasContentForCurrentRelease` derive the version from the bundle, so they pass at
  4.8 today and will pass after `deploy bump` writes 5.0 — the `"5.0"` teaser and bundled
  entry are both present.

### Registry golden tests

No golden test pinned qwen's `versionIntroduced` literal. `KimiSessionDiscoveryTests:8`
pins kimi at `"4.7"`, `NewProviderDiscoverabilityTests` pins cursor `"3.2"` and
codex/claude `"1.0"` — all historical, all untouched.
`testEveryVersionIntroducedProducesValidProviderHighlight` round-trips every source's
version through `providerHighlights(for:)` and now covers `"5.0"` automatically.

---

## 2. CHANGELOG — the `## [5.0]` section as written

`[Unreleased]` is kept above, now empty. `## [5.0] - 2026-08-17` (date is a placeholder;
`deploy` may restamp it).

Derivation: `git log 0e2cb747..HEAD` — the registry program (`f75d58ee`…`7da5aca7`), Qwen
(`ef4e9253`, `8c499ce7`), the contribute card (`e442dd23`, `d4f3f7a5`), the indexing fixes
(`67ecd8a1`, `d4958be3`), and the format-monitoring tooling (`4984b936`, `1dc36245`,
`4fba1961`, `348f7f2e`, `05c910fe`).

### `### Highlights` (2)

1. **Every agent Agent Sessions reads now runs on one shared foundation.** The registry /
   catalog architecture, framed as the user-visible consequence — an agent could appear on
   one surface and be missing from another; all surfaces now derive from one source
   description; adding an agent is a documented recipe. Names the 5.0 reason.
2. **Qwen Code joins as the thirteenth session source.** Carries the honest verification
   note: format verified against real **0.14.3** transcripts; installed CLI is 0.21.13 but
   the free OAuth tier was withdrawn 2026-04-15, so no fresh 0.21.13 transcript exists and
   resume is implemented from help/reader evidence plus hermetic tests, never run end to
   end. (Sourced from `docs/agent-support/agent-support-matrix.yml:194,207-216` and
   `agent-support-ledger.yml:18-21`, not from the prior draft's looser phrasing.)

### `### Features` (1)

- **A one-time invitation to add a missing agent.** The contribute card, carried over from
  the previous `[Unreleased]` bullet unchanged.

### `### Bug Fixes` (5) — §8.1–8.5 plus the ingest fix

| Bullet | Origin | Shipped broken in |
|---|---|---|
| Switching an agent off keeps it out of filter-view search results | §8.5 | 4.8 and earlier |
| Kimi Code and Grok CLI report when loading has finished | §8.1 / §8.2 | 4.7 / 4.8 |
| OpenClaw no longer sets Analytics rebuilding on every launch | §8.3 | 4.8 and earlier |
| Every agent's switch shows the same "restart to apply" notice | §8.4 | 4.8 and earlier |
| Database-backed agent sessions now enter indexed search | OpenCode/Hermes ingest identity | since 2.8 / 3.7 |

### `### Documentation` (1)

- The manual contribution path, extended with one sentence on the per-agent steward
  program.

### `### Maintenance` (3) — internal, behind the generator-drop comment

Format-monitoring tooling (Grok prebump driver lifting grok to `supports_latest`, the
format check's value pass and install-then-session bump rule, single-sourced matrix-key
map, Qwen registered in the monitored set 11 → 12), `scripts/steward_check.py`, and the
registry test sentinels. Placed under the existing HTML comment explaining that headings
the generator does not recognise are dropped from Sparkle and the GitHub body — verified
against `tools/release/sparkle_release_notes.py:287-320`, which only reads Highlights /
Features / Improvements / Bug Fixes.

### Deliberately NOT listed (Iron Rule)

- The **Qwen empty-context indexing crash** (`67ecd8a1` C1) — introduced and fixed inside
  this cycle, folded into the Qwen feature.
- The **FTS `LIMIT -1` regression** and its bounded-sort restoration (`67ecd8a1` C2) — the
  commit itself states unfiltered callers are byte-identical to pre-Qwen, so no shipped
  behavior was ever broken. Folded.
- The **exact FTS eligibility pushdown** (`d4958be3` C2 precision) — a precision
  improvement to the same pre-release work. Folded.
- **Every contribute-card fix round** (`d4f3f7a5` aging + copy) — refinements to a feature
  that first ships in 5.0. Folded into its one Features line.
- Qwen review importants I2–I8 — internal hardening, dropped.

This is the 4.8 lesson applied: four Grok defects were listed as Bug Fixes in 4.8 that no
user had ever received. Nothing of that shape survives here.

---

## 3. What's New (in-app)

`WhatsNewCatalog` gains a `"5.0"` teaser and four bundled rows.

**Teaser:** "Qwen Code joins the lineup, and every agent Agent Sessions reads now runs on
one shared foundation."

**Authored highlights (3) + support row**, following the 4.7/4.8 entry shape:

1. `square.stack.3d.up` — **One foundation under every agent**
2. `person.2.badge.plus` — **Add the agent you use** (contribute card + steward program)
3. `switch.2` — **The agent switches reach everywhere** (§8.3/8.4/8.5, phrased as what a
   user notices)
4. `heart.fill` — **Support the project** (verbatim from 4.7/4.8)

**Qwen's row is not authored.** `providerHighlights(for: "5.0")` generates
`New: Qwen Code` from `SessionSource.qwen.versionIntroduced == "5.0"`. A comment above the
`"5.0"` bundled array states this, matching the comment 4.8 carries for Grok.

Assembled order is pinned by a new test:
`One foundation… → Add the agent you use → The agent switches… → New: Qwen Code →
Support the project`, with `hasContent(for: "5.0") == true` asserted in the same test.

**Tests touched:**
- `testQwenReleaseHas49TeaserAndProviderHighlight` → `testQwenReleaseHas50TeaserAndProviderHighlight`.
  The exact teaser-string equality was relaxed to `XCTAssertNotNil` (the copy is now
  editable without a test edit); the provider-row assertion is unchanged.
- New: `testAuthoredHighlightsLeadProviderAndSupportRowsIn50`, the 5.0 twin of
  `testAuthoredHighlightsLeadProviderAndSupportRowsIn48`.

---

## 4. Release copy drafts

`docs/superpowers/plans/2026-08-17-release-5.0-NOTES.md` — marked DRAFT throughout.
Contains the GitHub release body, the Sparkle expectation (what the generator should emit,
for the approval gate), and the README "New in 5.0" replacement block covering both the
badge line at `README.md:39` and the `## What's New in 4.8` section at `README.md:81-92`,
including the `#whats-new-in-48` → `#whats-new-in-50` anchor change. Three open questions
for the owner are listed at the end.

The README block is a draft in that file rather than an applied edit, because it replaces
launch-facing copy the owner may want to shape. The only README line changed in place was
`:102`, which was a factual "shipping in 4.9" stamp.

---

## 5. Verification

| Step | Result |
|---|---|
| Debug build (`-derivedDataPath .deriveddata-run`) | `** BUILD SUCCEEDED **` |
| Focused Onboarding tests | `OnboardingContributeCardTests` passed; `WhatsNewCatalogTests` 11 tests, 0 failures |
| Renamed / new tests ran | `testQwenReleaseHas50TeaserAndProviderHighlight` passed; `testAuthoredHighlightsLeadProviderAndSupportRowsIn50` passed |
| `./scripts/xcode_test_stable.sh` | **2041 executed, 0 failures, 3 skipped** — `** TEST SUCCEEDED **` |
| `git diff --check` | clean (exit 0) |
| Residual `4.9` in shipping code/docs | none |

---

## 6. Files changed

| File | +/- |
|---|---|
| `AgentSessions/Model/SessionSource.swift` | +1 / -1 |
| `AgentSessions/Onboarding/Models/WhatsNewCatalog.swift` | +32 / -1 |
| `AgentSessionsTests/OnboardingFeedbackTriggerTests.swift` | +18 / -6 |
| `AgentSessionsTests/OnboardingContributeCardTests.swift` | +4 / -4 |
| `docs/CHANGELOG.md` | +21 / -4 |
| `docs/adding-a-session-source.md` | +2 / -2 |
| `docs/agent-support/agent-support-matrix.yml` | +1 / -1 |
| `README.md` | +1 / -1 (line 102 only) |
| `docs/superpowers/plans/2026-08-17-release-5.0-NOTES.md` | new |
| `docs/superpowers/plans/2026-08-17-release-5.0-PREP-REPORT.md` | new (this file) |

---

## 7. Concerns for the owner

1. **README What's New is a draft, not applied.** Section 3 of the NOTES file must be
   pasted into `README.md` before or during deploy, including the anchor rename. Left
   unapplied on purpose — it is launch copy.
2. **The CHANGELOG date is a placeholder** (`2026-08-17`). Restamp if the release slips.
3. **The Qwen honesty note is now longer than the previous draft** because it names the
   OAuth-tier withdrawal as the reason 0.21.13 is unverified. Trim it if the release copy
   should not carry that detail — but the verified version (0.14.3) should stay.
4. **`agent_sessions_version` in the support matrix now reads `5.0-unreleased`.** If the
   format-check tooling expects `deploy` to own that field, this edit may collide; it was
   retargeted because it is release-bound and the schema doc ties it to
   `MARKETING_VERSION`.
5. **Steward program framing.** The steward program appears as one sentence inside the
   Documentation entry and one clause in the What's New contribute row. If it is meant to
   be a launch story of its own, it needs its own Highlights bullet.
