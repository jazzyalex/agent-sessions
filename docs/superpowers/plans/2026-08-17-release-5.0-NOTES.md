# Agent Sessions 5.0 — release copy (DRAFT for owner review)

**Status: DRAFT. Nothing here is published.** No version numbers, appcast entries, or
Homebrew files were touched — `deploy` owns those. The three blocks below are the
user-facing copy for the 5.0 release, written per `.claude/skills/release-notes/SKILL.md`.

Source of truth is the `## [5.0]` section of `docs/CHANGELOG.md`; the deploy tool
generates the Sparkle and GitHub bodies from it. If the generated preview disagrees with
this file, the CHANGELOG section is what to edit.

Note on the date: the CHANGELOG section is stamped `2026-08-17`. Change it if the release
ships on a different day.

---

## 1. GitHub release body (DRAFT)

```markdown
## 🚀 New Features

### Major

- **Every agent Agent Sessions reads now runs on one shared foundation.** Each of the
  thirteen agents was wired in by hand, surface by surface — the session list, search,
  the filter pills, Analytics, the Settings panes, the launch-progress state, the
  enablement notice — which is why an agent could appear in one place and be quietly
  missing from another, with nothing to report the gap. There is now a single description
  of what a session source is, and every one of those surfaces derives from it. Switch an
  agent off and it leaves all of them together; a fix to one surface can no longer skip
  the rest. For anyone adding an agent, the job is a documented recipe with its own tests
  rather than fourteen scattered edits that had to be found first. This is why the version
  number moves to 5.0.

- **Qwen Code, the thirteenth agent source.** Qwen's local JSONL chats join browsing,
  search, filtering, Analytics, saved-session backfill, and the transcript view, with
  recorded reasoning and tool activity. Rewound branches are reconstructed along Qwen's
  active parent chain, and current custom titles and working directories follow that
  chain. Settings gains binary and storage-root controls. Active chats offer resume via
  the CLI's `--resume <id>`, with directory-scoped `--continue` as a fallback; archived
  histories stay browse-only.

  The format is verified against real 0.14.3 transcripts. The installed CLI is 0.21.13,
  but Qwen's free OAuth tier was withdrawn on 2026-04-15, so no fresh 0.21.13 transcript
  could be captured — resume is implemented from the installed CLI's help and reader
  behavior plus hermetic tests, and has not been run end to end.

### Moderate

- **A one-time invitation to add a missing agent.** If Agent Sessions has been on your Mac
  a while, a single dismissible card appears above the session list pointing at the
  source-proposal form and the contributing guide. It shows up only after 25 opened
  sessions or 45 days, never on a fresh install, never above another card, and retires
  permanently on dismissal, a second "Maybe later", or the moment you open either page.
  It reads nothing and sends nothing — it only opens a public page.

  Agents that ship can also pick up a **steward**: someone who keeps that agent's format
  verified as its CLI moves. See [STEWARDS.md](STEWARDS.md).

## 🐞 Bug Fixes

### Major

- **Switching an agent off now keeps it out of filter-view search results.** Search inside
  the filtered session view ran on a hand-written allow-list that stopped short of several
  agents, so their sessions could still surface in results after you had disabled them.

- **Database-backed agent sessions now enter indexed search.** OpenCode and Hermes
  sessions stored in SQLite shared one file path, so background search ingest could not
  tell their session identities apart and skipped their transcript text entirely. Ingest
  now tracks each session's identity, storage path, and content revision independently,
  picks up WAL-only changes, and reconciles moved, unpinned, deleted, and old-root
  sessions across both search and Analytics.

### Moderate

- **Kimi Code and Grok CLI report when their loading has finished.** Neither agent's
  indexing phases reached the app's launch state, so startup progress could stay
  incomplete while their sessions were already ready. An agent you do not have installed
  now settles to ready instead of hanging.

- **OpenClaw no longer sets Analytics rebuilding on every launch.** Its enable switch was
  mirrored into a second stored value that was never seeded from the real setting, so each
  launch read a change that had not happened and triggered a refresh nothing asked for.

- **Every agent's switch shows the same "restart to apply" notice.** Toggling Hermes,
  Droid, OpenClaw, Cursor or Pi silently skipped the notice the other agents flash.
```

---

## 2. Sparkle release notes (DRAFT)

Generated by `tools/release/sparkle_release_notes.py` from the CHANGELOG `[5.0]` section.
What the generator should emit, for the approval gate to check against:

**Highlights** (leads, from `### Highlights`)
1. Every agent Agent Sessions reads now runs on one shared foundation.
2. Qwen Code joins as the thirteenth session source.

**Features**
- A one-time invitation to add a missing agent.

**Bug Fixes**
- Switching an agent off now keeps it out of filter-view search results.
- Kimi Code and Grok CLI report when their loading has finished.
- OpenClaw no longer sets Analytics rebuilding on every launch.
- Every agent's switch shows the same "restart to apply" notice.
- Database-backed agent sessions now enter indexed search.

`### Documentation` and `### Maintenance` are headings the generator does not recognise,
so they are dropped from Sparkle and the GitHub body by design. That is intentional: the
steward tooling, the format-monitoring drivers, and the registry test sentinels are
development history, not a delta anyone updating from 4.8 experiences.

**Approval-gate check:** no line here describes something introduced and fixed inside this
cycle. Specifically excluded — the Qwen empty-context indexing crash, the FTS `LIMIT`
regression and its bounded-sort restoration, and every round of contribute-card fixes.
All were built and repaired between 4.8 and 5.0; no user ever received them.

---

## 3. README replacement block (DRAFT)

Replaces the badge line at README.md:39 and the whole `## What's New in 4.8` section
(README.md:81–92). Anchor changes from `#whats-new-in-48` to `#whats-new-in-50`.

### 3a. Badge line (replaces line 39)

```markdown
> **New in 5.0** — every agent Agent Sessions reads now runs on one shared foundation, Qwen Code joins as the thirteenth agent source, and there is a written path for adding the agent you use. [See what's new ↓](#whats-new-in-50)
```

### 3b. What's New section (replaces lines 81–92)

```markdown
## What's New in 5.0

**TL;DR** - Thirteen agents used to be wired in by hand, one surface at a time; they now
run on one shared foundation, which is why this release is 5.0. Qwen Code joins as the
thirteenth source, and adding a fourteenth is a documented recipe anyone can follow.

New in 5.0:
- **One foundation under every agent** — the session list, search, the filter pills,
  Analytics, the Settings panes and the launch state each carried their own hand-written
  list of agents, so an agent could appear in one and be quietly missing from another.
  They all derive from a single source description now. Switching an agent off reaches
  every surface at once, and adding an agent is a documented, tested recipe rather than
  fourteen scattered edits.
- **Qwen Code, the thirteenth agent source** — local JSONL chats in browsing, search,
  filtering, Analytics and the transcript view, with reasoning and tool activity, rewound
  branches reconstructed along the active parent chain, and Settings controls for binary
  and storage root. Active chats offer `--resume`; archived histories stay browse-only.
  Format verified against real 0.14.3 transcripts.
- **Add the agent you use** — a proposal form, an implementation brief you can hand to
  your own coding agent, and a PR template, surfaced once from inside the app by a
  dismissible card. Shipped agents can pick up a steward who keeps their format verified
  as the CLI moves.
- **The agent switches reach everywhere** — disabled agents no longer surface in
  filter-view search results, Kimi and Grok report when their loading has finished, and
  OpenClaw stops marking Analytics out of date on every launch.
- **OpenCode and Hermes transcripts are searchable** — both store sessions in SQLite
  behind one shared file path, and search ingest could not tell their sessions apart, so
  their transcript text was never indexed.

**Recent releases** — 4.8: Grok CLI joins as the eleventh agent source, and Analytics
counts every agent you have enabled. 4.7: Kimi Code joins as the tenth source, and active
Claude cloud sessions appear in the Quota Meter. 4.6.4: Compact and Full Agent Cockpit
retired, leaving the Quota Meter as the only mode. 4.6: paste-a-cookie Claude web usage,
no CLI or Full Disk Access needed. Full history in the [changelog](docs/CHANGELOG.md).
```

### 3c. Also check when applying

- README.md:45 (Overview) already lists Qwen Code — no change needed.
- README.md:102 now reads "(new in 5.0)" — was "(shipping in 4.9)".
- Any other in-page link to `#whats-new-in-48` must move to `#whats-new-in-50`.

---

## Open questions for the owner

1. **Release date** — the CHANGELOG stamps 2026-08-17. Adjust at deploy time if it slips.
2. **Steward-program prominence** — the copy above treats the steward program as part of
   the contribute story rather than its own headline. Promote it if the launch messaging
   wants it standing alone.
3. **The 5.0 framing** — every block leads with the shared-foundation change as the reason
   the major number moves. If the launch would rather lead with Qwen, the CHANGELOG
   `### Highlights` order needs swapping first, since the generator follows it.
